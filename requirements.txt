# Web 框架与服务器（API用）
fastapi>=0.100.0
uvicorn>=0.22.0

# LangChain 核心与扩展
langchain==1.3.4
langchain-classic==1.0.7
langchain-community==0.4.2
langchain-core==1.4.1
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
# 修改点 1：将旧版本 0.1.0 升级为兼容 LangChain 1.x 的版本
langchain-huggingface>=1.0.0,<2.0.0

# 向量数据库
chromadb==0.5.5

# 嵌入模型与重排序模型
sentence-transformers==3.0.1
modelscope>=1.10.0

# Ollama 客户端
# 修改点 2：放宽版本，让 langchain-ollama 1.1.0 能正常调用新版 SDK 交互
ollama>=0.6.1,<1.0.0

# Pydantic 及兼容包
# 修改点 3：将限制锁死在 2.5.3 调整为满足 LangChain 1.x 要求的范围
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# 其他依赖（隐式需要）
# 修改点 4：注释掉过旧的 hub 锁定版本，防止 sentence-transformers 产生依赖报错，交由 pip 自动向下兼容解析
# huggingface-hub==0.16.4